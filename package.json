{
  "name": "project-1_pocket-adviser",
  "version": "1.0.0",
  "description": "App that helps people maintain F1 status (student status) specifically for international students.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint --quiet .",
    "fix": "eslint --fix .",
    "test": "npm run lint && cross-env NODE_ENV=test mocha -u bdd --reporter spec --exit"
  },
  "license": "ISC",
  "dependencies": {
    "chart.js": "^2.9.4"
  },
  "devDependencies": {
    "prettier": "^1.17.1"
  },
  "engines": {
    "node": "12.4.0"
  }
}
